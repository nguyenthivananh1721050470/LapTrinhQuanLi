@model IEnumerable<LapTrinhQuanLi.Models.HeDaotao>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHeDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHeDT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHeDT }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHeDT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHeDT })
        </td>
    </tr>
}

</table>
